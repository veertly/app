export const MAX_PARTICIPANTS_GROUP = 4;
export const DEFAULT_EVENT_OPEN_MINUTES = 30;
export const DEFAULT_EVENT_CLOSES_MINUTES = 60;

export const DEFAULT_KEEP_ALIVE_INTERVAL = 45000; // after this amount of miliseconds, a new keep alive message is sent
export const DEFAULT_OFFLINE_INTERVAL = 60000; // after this amount of miliseconds of not being seen, the user is considered as offline
